import styled from 'styled-components';

export const UploadBtn = styled.button`
  border: none;
  background: none;
  display: flex;
  margin-top: auto;
  z-index: 1;
  position: absolute;
  transform: translateX(50%);
  top: 8.25rem;
`;
